{
	"name": "archmac-site",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Web oficial y documentación de ArchMac (Astro + Starlight).",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"build:all": "pnpm --dir ../simulation build && astro build && node ./scripts/copy-demo.mjs",
		"preview": "astro preview",
		"check": "astro check"
	},
	"dependencies": {
		"@astrojs/starlight": "^0.41.5",
		"@fontsource-variable/inter": "^5.3.0",
		"astro": "^7.1.6"
	}
}
